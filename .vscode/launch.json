{
  "configurations": [
      {
          "type": "node",
          "name": "Launch via Node",
          "request": "launch",
          "cwd": "${workspaceFolder}",
          "runtimeArgs": ["."],
      }
  ]
}